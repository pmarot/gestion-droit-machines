<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Liste des droits</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <% include ../partials/head %>
        <!-- TODO le style-->
        <link rel="stylesheet" type="text/css" media="screen" href="../../public/stylesheets/machines.css" />
</head>

<body>
    <div class="container">
        <h1 class="text-center font-weight-bold mb-5 mt-5 titre">Liste des droits</h1>
        <% if(droits.length > 0){%>
            <table class="table table-dark text-center">
                <thead>
                    <tr>
                        <th class="h3 font-weight-bold" scope="col">ID Machine</th>
                        <th class="h3 font-weight-bold" scope="col">ID Utilisateur</th>
                        <th class="h3 font-weight-bold" scope="col">Droit</th>
                        <th scope="col" editer</th>
                    </tr>
                </thead>
                <tbody>
                    <% for (i=0; i<droits.length; i++) { %>
                        <tr>
                            <td>
                                <%= droits[i].id_machine.nom %>
                            </td>
                            <td>
                                <%= droits[i].id_utilisateur.nom %>
                            </td>
                            <td class="p-3">
                                <a class="link_edit text-uppercase border p-2" href="/droits/edit/<%= droits[i]._id %>">Editer</a>
                            </td>

                        </tr>
                        <% } %>
                </tbody>
            </table>
            
            <div>
                <a class="btn btn-primary" href="/droits/create">Creer des droits</a>
            </div>

            <%} else{ %>
                <div>Il n'y a pas de machine enregistré</div>
                <div>
                    <a href="/droits/create">Creer des droits</a>
                </div>
                <% } %>
    </div>
</body>

</html>